sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        config:
          loaded_at_field: _etl_loaded_at
          freshness:
            warn_after:
              count: 1
              period: minute
            error_after:
              count: 6
              period: hour
      - name: orders
        config:
          loaded_at_field: _etl_loaded_at
          freshness:
            warn_after:
              count: 1
              period: minute
            error_after:
              count: 6
              period: hour